# 理解本质：
- 数据结构的目的就是 预先存储、维护操作中需要用到的信息，为后续操作达到减低复杂度；
- 不同的数据结构对于数据维护储存的方式也不同，这也就决定了这种数据结构的特点
- 而对于数据所需要的不同处理也决定数据结构的选择；

# 一句话总结：

**数据结构 = 一套信息维护规则。修改数据时付出代价维护规则，让后续高频操作变得高效。选型就是看你的高频操作是什么。**

可以延伸：算法是操作逻辑，数据结构是数据的组织形式；算法跑在数据结构之上。