# 01背包
本质上是 : 对于一个给定一个限制性条件，在这个限制性条件中每个元素具有选择性，在选择性中规划出极值答案
有下面常见的3中情况：
![](../../../截图文件/屏幕截图%202026-04-07%20093348.png)
一般是关于求这3种问题：1.求最大   2.求最小   3.求方案数
这三种决定了他们不同的状态转移方程：(递推)

二维`dp`：
->这里个人更加偏向写二维`dp`数组
好处：
- 每一次状态递推的过程是相邻的层级关系，没有了优化后的状态压缩
坏处：
- 有内存开销更大

1.求`min/max`
```c++
if(j < nums[i]) dp[i][j] = dp[i - 1][j];//只能不选

// 但在求最小值时，若无法不选（必须装满），此状态应设为无穷大//非法状态

else dp[i][j] = max(dp[i - 1][j],dp[i - 1][j - nums[i]] + nums[i]);

 //dp[i][j] = min(dp[i-1][j], dp[i-1][j-nums[i]] + nums[i]);
注意：
当然不一定加nums[i]
求max值时，+= nums[i];
求max_len时，+= 1;//跟着题目来

```
**初始化陷阱：求最小值时，DP 数组通常需要初始化为一个极大值（如 `0x3f3f3f3f`(INF)），而不是 0，因为我们要找 “最小” 的代价。(max同理)**
也就是非法状态要极限化，使得每一次状态转移时非法状态都没选上**
2.求方案数
```c++
dp[0][0] = 1;
if(j < nums[i]) dp[i][j] = dp[i - 1][j];

else dp[i][j] = d[i - 1][j] + dp[i - 1][j - nums[i]];
```


**但是如果这里有内存溢出：二维优化成一维/改成滚动数组(mod 2)**
一维`dp`：
1.求`min/max`
```c++
for(int i = 0;i < n;i++) {

	for(int j = m;j >= nums[i];j--) { 
		//采用倒序更新就可以保证：
		//左侧节点依旧为旧数据，在该节点的计算都是基于旧数据上，避免在该节点上重复计算了左侧数据
		
		//这里对于if(j < nums[i]) dp[j] = dp[j];没有意义，删去
		
		if(j >= nums[i]) dp[j] = max(dp[j],dp[j - nums[i]] + nums[i]);
	}
}
```
2.求方案数：
```c++
//思路同理
dp[0] = 1; // 👈 这一句就是【唯一必须的初始化】
for (int i = 0; i < n; i++) {

	for (int j = m; j >= nums[i]; j--) {
		
		dp[j] += dp[j-nums[i]];
	}
}
```


# 完全背包
其本质是就是01背包，只不过状态转移方程有所不同；
![](../../../截图文件/屏幕截图%202026-04-08%20170918.png)
两者的递推思路是点小区别：
->这里就只展示二维`dp`递推（**初始化和优化要注意的点和01背包相同**）：
1.求`min/max`
```c++
//求max
if(j < nums[i]) dp[i][j] = dp[i - 1][j];//只能不选

else dp[i][j] = max(dp[i - 1][j],dp[i][j - nums[i]] + nums[i]);

注意：
当然不一定加nums[i]
求max值时，+= nums[i];
求max_len时，+= 1;//跟着题目来
```
2.求方案数
```c++
dp[0][0] = 1;
if(j < nums[i]) dp[i][j] = dp[i - 1][j];

else dp[i][j] = d[i - 1][j] + dp[i][j - nums[i]];
```

# 分组背包：

**本质：和0-1背包并无两样**

可以这样对比：0-1背包和组合背包的区别是：
 - 0-1背包每一层因只有一个元素，所以只是每层依次枚举该元素即可；
 - 组合背包每一层不再是一个元素了，而是一个集合(一个集合里面可能存在多个元素)，每层则是需要遍历集合中所有可能选择的元素;

总结下来：
- 说白了，组合背包就是在0-1背包的基础上因为每一层不在是单一元素选项，存在多个元素选项，在加一层暴力枚举集合中的元素就是了，其他的同理；

## 注意：

这里有个**易错点** : 在倒序遍历一层中的背包大小时一定要**注意顺序** -- 是先遍历完这一层集合中所有可以选择元素，再去遍历（前一个/下一个）节点;

解释：和用一维数组优化的倒序遍历的解释相同，都是因为一层中至多只可以选一次，避免在一层中出现数据迭代混乱（准确来说：如果不是按照该顺序该层的节点信息就有可能是因为选择了多个元素累加出来的数据，从而发生数据混乱）;

## 例题
- [4040. 构造子集和的最少操作次数 I](https://leetcode.cn/problems/minimum-operations-to-form-subset-sum-i/)
- [4041. 构造子集和的最少操作次数 II](https://leetcode.cn/problems/minimum-operations-to-form-subset-sum-ii/)
两个题目大同小异：
区别在于每层一个元素对应所等产生的集合，在题目条件的不同下，所能产生的子元素可能不同，但是在算法逻辑上是一致的；

### 代码参考：
[4040. 构造子集和的最少操作次数 I](https://leetcode.cn/problems/minimum-operations-to-form-subset-sum-i/)
```c++
class Solution {
public:
    //分组背包
    int minOperations(vector<int>& nums, int sum) {
        vector<long long> dp(sum + 1,INT_MAX);
        dp[0] = 0;
        for(auto& x : nums) {
            int w = bit_width((size_t) x);
            for(int i = sum;i >= 0;i--) {
	            //遍历该层集合：
                //一维数组优化：
                //1. (元素大小 > i) -> dp[j] = dp[j],无意义;
                //2. 只看(元素大小 <= i) 即可;
                //遍历每个集合的选择情况：
                for(int a = 0;(x << a) <= i;a++) {
                    dp[i] = min(dp[i],dp[i - (x << a)] + a);
                }
                
                for(int a = w - 1;a && (x >> a) <= i;a--) {
                    dp[i] = min(dp[i],dp[i - (x >> a)] + a);
                }
            }
        }
        return dp[sum] == INT_MAX? -1 : dp[sum];
    }
};
```

[4041. 构造子集和的最少操作次数 II](https://leetcode.cn/problems/minimum-operations-to-form-subset-sum-ii/)
```c++
class Solution {
public:
    int minOperations(vector<int>& nums, int sum) {
        int n = nums.size();
        vector<long long> dp(sum + 1,INT_MAX);
        dp[0] = 0;
        for(auto& x : nums) {
            for(int i = sum;i >= 0;i--) {
                //遍历该层集合：
                for(int t = 0;x >> t;t++) {
                    for(int a = 0;(x >> t << a) <= i;a++) {
                        dp[i] = min(dp[i],dp[i - (x >> t << a)] + a + t);
                    }
                }
            }
        }
        return dp[sum] == INT_MAX ? -1 : dp[sum];
    }
};
```
